{
    "name": "projeto-rt",
    "private": true,
    "workspaces": [
        "frontend-rt",
        "backend-rt"
    ],
    "scripts": {
        "build:frontend": "cd frontend-rt && ng build",
        "build:backend": "cd backend-rt && npm run build",
        "build": "npm run build:frontend && npm run build:backend"
    },
    "devDependencies": {
        "@angular/cli": "^19.1.8",
        "@angular-devkit/build-angular": "^19.1.8",
        "typescript": "~5.7.2",
        "npm-run-all": "^4.1.5",
        "concurrently": "^8.2.0",
        "wait-on": "^7.0.1"
    }
}